# ⚡ Упрощённая Настройка Preview - Используем Test Режим

## 🎯 Упрощённый Подход

Вместо создания отдельной Neon ветки, используем **test mode** через T-Bank DEMO терминал.
Это безопасно, так как:
- ✅ Тестовые платежи не создают реальных транзакций
- ✅ Тестовые пользователи изолированы по `device_id`
- ✅ Можно легко очистить тестовые данные после проверки

---

## 📋 Минимальная Настройка (2 шага)

### Вариант 1: Общая База + Test Mode (РЕКОМЕНДУЕТСЯ)

**Преимущества:**
- ⚡ Быстро (2 минуты настройки)
- 🔒 Безопасно (DEMO терминал)
- 🧹 Легко очистить тестовые данные

**Что настроить в Vercel:**

#### Environment Variables для Preview

**Vercel Dashboard → PinGlass → Settings → Environment Variables**

Добавить **ТОЛЬКО 2** переменные для Preview environment:

| Variable | Value | Environment |
|----------|-------|-------------|
| `TBANK_TERMINAL_KEY` | `1765404063316DEMO` | **Preview** |
| `TBANK_PASSWORD` | ваш demo пароль | **Preview** |

**Важно:** Environment = **Preview** (не Production!)

**Всё остальное** (DATABASE_URL, Kie AI, R2, QStash, Sentry) **наследуется из Production**.

---

### Вариант 2: Отдельная База (через Vercel Neon Integration)

Если нужна полная изоляция данных:

#### Шаг 1: Создать Preview Branch через Vercel

1. **Vercel Dashboard** → PinGlass → **Storage**
2. Найти **Neon** integration
3. **Create Branch** → Name: `preview-test`
4. Скопировать **DATABASE_URL** для preview branch

#### Шаг 2: Настроить Environment Variables

| Variable | Value | Environment |
|----------|-------|-------------|
| `DATABASE_URL` | URL из preview branch | **Preview** |
| `TBANK_TERMINAL_KEY` | `1765404063316DEMO` | **Preview** |
| `TBANK_PASSWORD` | ваш demo пароль | **Preview** |

---

## 🚀 После Настройки

### Автоматический Deploy

Vercel автоматически создаст preview deployment:
```
https://pinglass-git-preview-reference-photos-5-8-your-team.vercel.app
```

### Test Checklist

**Upload (5-8 фото):**
- [ ] 4 фото → ❌ "минимум 5"
- [ ] 5 фото → ✅ кнопка активна
- [ ] 8 фото → ✅ 100% progress
- [ ] 9 фото → ❌ "максимум 8"

**File Size (30MB):**
- [ ] 20MB → ✅ загружается
- [ ] 35MB → ❌ "максимум 30MB"

**Payment (T-Bank DEMO):**
- Test card: `4300000000000777`
- [ ] Оплата → ✅ test page
- [ ] Status → ✅ `paid: true`

---

## 🧹 Cleanup После Тестирования

### Вариант 1 (Общая база):
```sql
-- Удалить тестовых пользователей (опционально)
DELETE FROM users WHERE device_id LIKE 'test_%';
DELETE FROM avatars WHERE user_id NOT IN (SELECT id FROM users);
```

### Вариант 2 (Отдельная база):
1. Vercel Dashboard → Storage → Neon
2. Delete branch `preview-test`

---

## 📊 Рекомендация

**Используйте Вариант 1** (общая база + test mode):
- ⚡ Быстрее настраивать
- 🔒 Безопасно (DEMO терминал)
- 🧹 Проще cleanup
- 💰 Экономит Neon ресурсы

**Используйте Вариант 2** только если:
- Нужна полная изоляция данных
- Планируете длительное тестирование
- Хотите протестировать миграции БД

---

## ✅ Quick Start

### Минимальная конфигурация (2 минуты):

1. **Vercel** → PinGlass → Settings → Environment Variables
2. Добавить для **Preview**:
   - `TBANK_TERMINAL_KEY` = `1765404063316DEMO`
   - `TBANK_PASSWORD` = ваш пароль
3. ✅ Готово! Vercel пересоберёт preview

---

**Дата:** 2025-12-30
**Ветка:** `preview/reference-photos-5-8`
**Статус:** Упрощённая конфигурация готова
